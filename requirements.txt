# =============================================================================
# Clarify Engine AI — runtime dependencies
# =============================================================================
#
# Cloud / Streamlit Cloud install order (CPU-only torch must be installed
# from the official PyTorch index, otherwise sentence-transformers will pull
# in the CUDA build and break the deployment slot):
#
#   pip install --no-cache-dir -r requirements.txt
#   pip install --no-cache-dir torch --index-url https://download.pytorch.org/whl/cpu
#
# Local dev install (any platform with a working PyTorch wheel):
#
#   pip install --no-cache-dir -r requirements.txt
#
# =============================================================================

streamlit>=1.32

# Core data handling
pandas>=2.1
openpyxl>=3.1

# Config / serialization
PyYAML>=6.0

# RAG core (MVP — must be installed in production and CI)
rank_bm25>=0.2.2
chromadb>=0.4.24
sentence-transformers>=2.7.0
# Optional transformers vision backends import this package; keeping it
# explicit avoids noisy ModuleNotFoundError warnings in Streamlit logs.
torchvision>=0.18.0

# LLM HTTP transport
requests>=2.31
pydantic>=2.7
fastapi>=0.110
uvicorn>=0.29

# Logging / CLI niceties
tqdm>=4.66

# Document parsing (Knowledge Base)
pypdf>=5.0.0
python-docx>=1.0.0

# .env loader for the KB Test UI (src/ui/app.py)
python-dotenv>=1.0.0
