# 4. Устранение проблем

> 📘 **Справка для БА** · файл 4 из 4
> ← [3. Результаты и экспорт](03_results_and_export.md) · К содержанию → [README](README.md)

Иногда что-то идёт не по плану: появляется красная плашка, не приходит ответ,
не скачивается файл. Эта страница объясняет, **что значит каждое
уведомление** и **что делать пошагово**.

---

## 🍞 Toast-уведомления: что значат иконки

«Toast» — это маленькое всплывающее уведомление в углу экрана. Оно появляется
на пару секунд и исчезает. Иконка в начале сообщения показывает суть.

| Иконка | Что значит | Когда появляется | Действия |
|:------:|------------|------------------|----------|
| ✅ | **Успех** | Запрос выполнен, ответ готов, файл сформирован | Ничего делать не нужно — продолжайте работу |
| 🧹 | **История очищена** | Вы нажали «Очистить историю» в чате | Можно начинать новый диалог |
| ⚠️ | **Внимание** | Запрос не сохранён / нет данных для повтора / промежуточная проблема | Прочитайте текст — там подсказка |
| ❌ | **Ошибка** | Не удалось получить ответ / сбой провайдера / сеть | См. раздел **«Ошибки LLM»** ниже |

> 💡 Если уведомление пропало раньше, чем вы успели прочитать, — посмотрите
> в основное окно: ошибка всегда дублируется отдельным блоком в чате или
> над кнопкой запуска.

---

## ❌ Ошибки LLM: «Не удалось получить ответ»

**Что вы видите:**

> ❌ **Не удалось получить ответ.**
> Причина: Все провайдеры недоступны
>
> **[Повторить]** **[📥 Скачать логи]**

### Что это значит на человеческом языке

Система пыталась обратиться к языковой модели (LLM), но **ни один из
доступных провайдеров не ответил**. Возможные причины:

- провайдер LLM временно перегружен или недоступен;
- проблема с интернет-соединением;
- закончились лимиты на запросы (квота API);
- временный сбой на стороне внешнего сервиса.

**Важно.** Ваш запрос **сохранён** в памяти системы. Не нужно перенабирать
вопрос или повторно загружать файл.

### Как работает кнопка «Повторить»

1. Вы нажимаете **«Повторить»**.
2. Система берёт **тот же самый запрос**, который не прошёл, и отправляет
   его заново.
3. Перед повтором система **сама** попробует следующего провайдера в
   цепочке (GigaChat → Ollama).
4. Если получилось — увидите ✅ и ответ.
5. Если снова не получилось — увидите ту же ошибку. Подождите 1–2 минуты и
   попробуйте ещё раз.

> ⏳ **Не жмите «Повторить» подряд десять раз.** Если первый повтор не
> помог — дайте провайдеру 1–2 минуты восстановиться.

---

## ⏱ Долгий первый ответ (60–90 сек на CPU)

**Что вы видите.** При самом первом запросе спиннер «Спрашиваем LLM…»
висит 60–90 секунд, а в обычной работе ответ приходит за 5–15 секунд.

### Почему так происходит

На CPU-only АРМ локальная Ollama при первом обращении в сессии должна
*поднять* модель `qwen2.5:7b` в оперативную память. Это разовая операция
длиной 60–90 сек на типовом АРМ. После прогрева модель остаётся в кэше
ещё 10 минут (`keep_alive=10m`), и все последующие запросы в этой сессии
возвращают ответ заметно быстрее.

> ✅ **Система не зависла.** Текст спиннера явно сообщает «Первый ответ
> на CPU может занять 60–90 сек.» — это ожидаемое поведение, см. BL-55
> (issue [#199](https://github.com/G-Ivan-A/clarify-engine-ai/issues/199)).

### Как ускорить первый запрос

В сайдбаре доступна кнопка **«🔥 Прогреть модель»**. Она отправляет в
локальную Ollama короткий фоновый запрос (`prompt="ok"`) и кладёт модель
в кэш на 10 минут — после этого первый «настоящий» вопрос возвращается за
5–15 сек.

| Условие | Кнопка видна |
|---------|-------------|
| `ui.debug_mode: true` в `configs/ui_config.yaml` | ✅ Да |
| `OLLAMA_BASE_URL=http://localhost:11434` (или `127.0.0.1`) | ✅ Да |
| `OLLAMA_BASE_URL=https://ollama.example.com` (облако/пилот) | ❌ Скрыта — не флудим прогревом чужой сервис |

**Что делать, если кнопка не помогла:**

1. Откройте окно CMD с `ollama serve` и убедитесь, что демон запущен.
2. Выполните `curl http://localhost:11434/api/tags` — Ollama должна
   ответить JSON со списком моделей.
3. Если кнопка пишет «Ollama не отвечает на прогрев» — см.
   [`docs/runbooks/arm-deployment-ivan.md`](../runbooks/arm-deployment-ivan.md) §6
   (Сценарий В: ошибка в UI), типовые причины — Ollama не запущена или
   занята длинным запросом.

> 💡 **Совет.** Запустите Ollama (`ollama serve`) и нажмите «🔥 Прогреть
> модель» сразу после запуска UI — тогда самый первый вопрос БА уже
> придёт в прогретую модель и не покажется «зависшим».

---

## ⚙️ `.env` не найден или сохранён как `.env.txt`

**Что вы видите:** при запуске `streamlit run src/ui/app.py` или
`python -m src.pipeline` появляется красное сообщение с фразой
«Обнаружен файл `.env.txt` вместо `.env`» либо «В `.env` отсутствуют
или пустые обязательные переменные: `OLLAMA_MODEL`, `OLLAMA_BASE_URL`».

### Что произошло

С BL-50 (issue #194) приложение **обязательно** проверяет `.env`
перед стартом. Возможные сценарии и реакция системы:

| Ситуация | Что делает приложение | Что нужно сделать вам |
|----------|-----------------------|------------------------|
| Файла `.env` нет, рядом лежит `.env.txt` | Останавливает запуск с подсказкой `ren .env.txt .env`. Silent rename запрещён — приложение **не** переименует файл само. | В Windows CMD выполнить `ren .env.txt .env` в каталоге проекта и снова запустить UI / pipeline. |
| `.env` нет, но есть `.env.example` | Автоматически копирует `.env.example` в `.env` и пишет в лог `Создан .env из .env.example`. | Ничего, можно продолжать. При необходимости откройте `.env` и заполните ключи провайдеров. |
| `.env` есть, но `OLLAMA_MODEL` или `OLLAMA_BASE_URL` пустые | Останавливает запуск со списком пустых переменных. | Открыть `.env` и подставить значения из `.env.example` (например, `OLLAMA_MODEL=qwen2.5:7b`, `OLLAMA_BASE_URL=http://localhost:11434`). |

> 💡 **Почему так строго?** Без guard'а ошибка ловится только в момент
> первого запроса к LLM — обычно через silent HTTP 404 от Ollama, что
> ведёт к 10+ минутам ложной диагностики «всё сломалось». Guard
> переводит её в детерминированную остановку при старте.

> ⚠️ **Notepad скрывает расширения.** Если вы редактируете `.env` через
> «Блокнот», в окне «Сохранить как» обязательно выберите
> `All Files (*.*)`, иначе Windows тихо добавит `.txt`. Подробнее —
> [`docs/runbooks/arm-deployment-ivan.md`](../runbooks/arm-deployment-ivan.md) §1.

---

## ⚙️ Изменения в `.env` / `configs/*.yaml` не применяются

**Что вы видите:** вы отредактировали `.env` (например, поменяли
`OLLAMA_MODEL` или `OLLAMA_TIMEOUT`) или один из файлов
`configs/*.yaml` (например, `ui_config.yaml`, `llm_config.yaml`),
обновили вкладку браузера со Streamlit — и видите **прежнее
поведение**: старая модель в ответе, прежний таймаут, прежняя
конфигурация UI.

### Что произошло

Streamlit при старте процесса один раз вызывает `load_dotenv()` и
один раз читает YAML-конфиги через `yaml.safe_load()`. Значения
остаются в памяти процесса до его завершения. Поэтому:

- Кнопка **«Rerun»** в правом верхнем углу Streamlit перезапускает
  только Python-код страницы, но **не** перечитывает `.env` и
  YAML-конфиги.
- `Ctrl+R` / `Ctrl+F5` / **обычное обновление вкладки** браузера —
  тоже не помогает: запрос идёт в тот же самый процесс с теми же
  значениями в памяти.
- Hot-reload Streamlit ловит изменения **только** в каталоге `src/`
  (см. [Streamlit docs — Caching & state](https://docs.streamlit.io/library/advanced-features/caching)).
  Конфиги в `configs/` и `.env` лежат вне этого каталога.

### Что нужно сделать

| Шаг | Действие | Где |
|-----|----------|------|
| 1 | Нажмите `Ctrl+C` в окне CMD, где запущен `streamlit run` | Windows CMD |
| 2 | Запустите Streamlit снова: `streamlit run src/ui/app.py` | Windows CMD |
| 3 | В браузере нажмите `Ctrl+Shift+R` — это «жёсткое» обновление с очисткой кэша браузера | Браузер |

После шага 3 откройте режим, в котором проверяете изменение, и
убедитесь, что новое значение действительно подхвачено (например,
в чате видна другая модель, или новый таймаут отображается в
логе при первом запросе).

> 💡 **Почему `Rerun` не достаточно?** Streamlit `Rerun` спроектирован
> для быстрой повторной отрисовки UI без потери состояния процесса —
> это полезно при правке `src/`, но это же поведение делает невидимым
> любое изменение в файлах, прочитанных однократно при старте.

> ⚠️ **Не путайте с `src/`.** Если вы правите Python-файлы в
> `src/`, Streamlit предложит автоперезагрузку — это другой
> механизм. Для `.env` и `configs/*.yaml` автоперезагрузки **нет**
> by-design.

Если после полного рестарта изменения всё ещё не применяются —
переходите к разделу **«Ошибки LLM»** ниже и скачайте логи через
кнопку «📥 Скачать логи»: вероятно, проблема в значении самого
ключа, а не в кэше.

Подробный сценарий для АРМ — [`docs/runbooks/arm-deployment-ivan.md`](../runbooks/arm-deployment-ivan.md)
§2 (предупреждение «BL-53 — кэш `.env` и `configs/*.yaml`») и §6
(типовая ошибка «Правка `.env` / `configs/*.yaml` не применилась»).

---

## 🆘 Частые проблемы и быстрые решения

### 1. Файл не загружается

| Симптом | Причина | Решение |
|---------|---------|---------|
| «Unsupported file type» | Формат не `.xlsx` / `.docx` | Пересохраните файл в правильный формат |
| Файл `.doc` отказывается грузиться | Старый бинарный формат не поддерживается | Откройте в Word, «Сохранить как…» → `.docx` |
| «File too large» (> 10 МБ) | Превышен лимит | Разбейте ТЗ на части или удалите тяжёлые медиа из документа |

### 2. Анализ запустился, но «висит»

| Симптом | Причина | Решение |
|---------|---------|---------|
| Прогресс долго стоит на одной строке | Длинное требование, медленный провайдер | Подождите 1–2 минуты — обычно проходит |
| Прогресс совсем не двигается > 5 минут | Сбой на стороне провайдера | Закройте вкладку, перезагрузите страницу и запустите заново |
| Появилась ошибка посередине | Один из вызовов LLM упал | Анализ продолжится, эту строку получите со статусом `Ошибка` — нажмите «Повторить только ошибки» |

### 3. Отчёт скачался, но в нём пусто / странно

| Симптом | Причина | Решение |
|---------|---------|---------|
| Много строк со статусом `Ошибка` | Серия сбоев провайдера | Запустить «Повторить только ошибки» по тому же RunID |
| Confidence у всех низкий | ТЗ сильно отличается от документации в КБ | Это **информация для вас**: системе мало известно о таком домене. Передайте этот вывод в команду — возможно, нужно пополнить базу знаний |
| Статусы только `НД` | База знаний не покрывает тему ТЗ | Уточните с командой, какие источники добавлены в КБ |

### 4. Чат-консультация молчит или отвечает невпопад

| Симптом | Причина | Решение |
|---------|---------|---------|
| Ответ не приходит несколько минут | Провайдер занят | Подождите, при сбое появится «❌ Не удалось получить ответ» и «Повторить» |
| Модель отвечает «не по теме» | В истории остались сообщения из предыдущей темы | Нажмите **«🧹 Очистить историю»** и задайте вопрос заново |
| Модель «галлюцинирует» — выдумывает | Ответ без цитат, низкий Confidence | Не верьте — проверьте, есть ли в ответе ссылка на источник; если нет — переформулируйте вопрос |

### 5. Кнопка «Повторить» не появилась, хотя ошибка была

| Симптом | Причина | Решение |
|---------|---------|---------|
| Ошибка показана, но «Повторить» нет | Запрос не успел сохраниться (редкий случай) | Перенаберите вопрос или повторно загрузите файл вручную |

---

## 📥 Кнопка «Скачать логи»

Под красной плашкой ошибки есть кнопка **«📥 Скачать логи»**.

**Что в файле:**

- идентификатор запроса (`run_id`);
- технические детали сбоя (без секретов и без вашего исходного текста в
  открытом виде — чувствительные данные маскируются).

**Когда нажимать:** только если вас об этом попросила служба поддержки.
Самостоятельно открывать и читать этот файл бизнес-аналитику **не нужно**.

---

## 🧭 Как сообщить о проблеме

Если вы столкнулись с **повторяющейся** ошибкой или странным результатом,
который не объясняется ни одним из пунктов выше:

1. **Сохраните контекст:**
   - запишите **RunID** (есть в отчёте и в логах);
   - сделайте скриншот экрана с ошибкой;
   - при наличии — скачайте логи кнопкой **«📥 Скачать логи»**.

2. **Создайте issue на GitHub:**
   👉 **<https://github.com/G-Ivan-A/clarify-engine-ai/issues>**

   В тексте укажите:
   - что вы делали (загружали файл, задавали вопрос, …);
   - что ожидали увидеть;
   - что увидели по факту;
   - RunID и скриншот.

3. **Или напишите в команду поддержки** по внутренним каналам (Slack,
   корпоративная почта).

> 📝 **Совет.** Не пишите «всё сломалось». Опишите **конкретный шаг**, на
> котором возникла проблема, и **что система ответила** — так разработчики
> починят быстрее.

---

## 🛟 Чек-лист самопомощи

Прежде чем писать в поддержку — пройдитесь по списку:

- [ ] Проверил(а) формат и размер файла (≤ 10 МБ, `.xlsx` или `.docx`).
- [ ] Нажал(а) «Повторить» один раз и подождал(а) 1–2 минуты.
- [ ] Перезагрузил(а) страницу браузера и попробовал(а) заново.
- [ ] В режиме «Консультация» очистил(а) историю и переформулировал(а)
      вопрос.
- [ ] Сохранил(а) RunID, скриншот и логи перед обращением в поддержку.

---

[← Назад: Результаты и экспорт](03_results_and_export.md) · [К содержанию →](README.md)
